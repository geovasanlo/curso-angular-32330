import { RepetirDirective } from './repetir.directive';

describe('RepetirDirective', () => {
  it('should create an instance', () => {
    const directive = new RepetirDirective();
    expect(directive).toBeTruthy();
  });
});
