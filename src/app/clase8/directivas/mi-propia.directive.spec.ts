import { MiPropiaDirective } from './mi-propia.directive';

describe('MiPropiaDirective', () => {
  it('should create an instance', () => {
    const directive = new MiPropiaDirective();
    expect(directive).toBeTruthy();
  });
});
